<nav class="navbar navbar-expand-lg navbar-light bg-light paper">
    <div class='container'>
        <div class="mr-auto">
            {% for slide in range(0, slides + 1) %}
                <button type='button' class='btn btn-secondary rounded-circle m-1 question-select'>{{loop.index}}</button>
            {% endfor %}
        </div>
        <span>
            <button type="button" class='btn btn-lg btn-primary submitButton next'>
                <span>Następne</span>
            </button>
        </span>
    </div>
</nav>